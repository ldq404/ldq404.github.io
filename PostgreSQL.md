
## 练习数据集

<details>
<summary>点击展开 / 收起</summary>

```sql
-- 用户表
CREATE TABLE users (
    id SERIAL PRIMARY KEY, -- 用户ID
    username VARCHAR(50) UNIQUE NOT NULL, -- 用户名
    nickname VARCHAR(50), -- 昵称
    email VARCHAR(255) UNIQUE, -- 邮箱
    phone VARCHAR(20) UNIQUE, -- 手机号
    age INTEGER NOT NULL CHECK (age >= 0 AND age <= 150), -- 年龄
    birthday DATE, -- 生日
    balance NUMERIC(12,2) NOT NULL DEFAULT 0 CHECK (balance >= 0), -- 账户余额
    is_active BOOLEAN DEFAULT TRUE, -- 是否启用
    created_at TIMESTAMPTZ DEFAULT NOW(), -- 注册时间
    updated_at TIMESTAMPTZ DEFAULT NOW(), -- 最后修改时间
    metadata JSONB -- 用户扩展信息
);

INSERT INTO users (username, nickname, age, balance)
VALUES ('zhangsan666', '张三', 23, 0),
       ('lisi888', '李四', 31, 2500, TRUE),
       ('wangwu', '王五', 19, 100, TRUE),
       ('zhaoliu', '赵六', 42, 5800, FALSE),
       ('xiaoming', '小明', 15, 0, TRUE),
       ('alice', 'Alice', 27, 12000, TRUE);
```

```sql
-- 收货地址表
CREATE TABLE addresses (
    id SERIAL PRIMARY KEY, -- 收货地址ID
    user_id INTEGER NOT NULL REFERENCES users(id), -- 用户ID
    receiver_name VARCHAR(20) NOT NULL, -- 收货人
    receiver_phone VARCHAR(20) NOT NULL, -- 收货电话
    province VARCHAR(20) NOT NULL, -- 省
    city VARCHAR(20) NOT NULL, -- 市
    district VARCHAR(20) NOT NULL, -- 区/县
    detail VARCHAR(255), -- 详细地址
    is_default BOOLEAN DEFAULT FALSE, -- 是否默认地址
    created_at TIMESTAMPTZ DEFAULT NOW() -- 创建时间
);

INSERT INTO addresses (user_id, receiver_name, receiver_phone, province, city, district, detail, is_default)
VALUES
    (1, '张三', '13800138001', '河北省', '石家庄', '长安区', '东风东路100号1单元101室', TRUE),
    (1, '张三', '13800138001', '北京市', '北京市', '朝阳区', '建国路88号SOHO现代城A座1205', FALSE),
    (6, '李四', '13900139001', '上海市', '上海市', '浦东新区', '世纪大道100号2栋802室', TRUE),
    (6, '李四', '13900139001', '河北省', '石家庄', '南市区', '朝阳北大街200号', FALSE),
    (7, '王五', '13700137001', '广东省', '深圳市', '南山区', '科技园科苑路66号', TRUE),
    (8, '赵六', '13600136001', '北京市', '北京市', '海淀区', '中关村大街50号', TRUE),
    (10, 'Alice', '13500135001', '浙江省', '杭州市', '西湖区', '文三路100号', TRUE),
    (10, 'Alice', '13500135001', '江苏省', '南京市', '鼓楼区', '中山北路200号', FALSE);

```

```sql
-- 商品分类表
CREATE TABLE categories (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, -- 分类id
    parent_id INTEGER REFERENCES categories(id), -- 父级id
    name VARCHAR(20) UNIQUE NOT NULL, -- 分类名称
    description TEXT, -- 分类描述
    is_active BOOLEAN DEFAULT TRUE, -- 是否启用
    created_at TIMESTAMPTZ DEFAULT NOW() -- 创建时间
);

INSERT INTO categories (name, description, parent_id, is_active, created_at)
VALUES
    ('电子产品', '手机、电脑、平板、耳机等电子设备', TRUE, '2026-08-20 10:00:00+08'),
    ('食品饮料', '零食、饮料、粮油等食品', TRUE, '2026-08-21 11:30:00+08'),
    ('服装鞋帽', '服装、鞋子、帽子及相关配饰', TRUE, '2026-08-22 09:15:00+08'),
    ('家居用品', '家具、厨具、清洁用品等', TRUE, '2026-08-23 14:20:00+08'),
    ('图书', '文学、技术、教育等各类图书', TRUE, '2026-08-24 16:45:00+08'),
    ('运动户外', '运动装备、户外用品等', TRUE, '2026-08-25 10:10:00+08'),
    ('美妆护肤', '护肤品、彩妆及个人护理用品', FALSE, '2026-08-26 13:00:00+08'),
    ('宠物用品', NULL, TRUE, '2026-08-27 15:30:00+08'),
    ('手机', '智能手机及相关设备', 1, TRUE, '2026-08-20 10:00:00+08'),
    ('平板电脑', '平板电脑及相关设备', 1, TRUE, '2026-08-21 11:30:00+08'),
    ('耳机', '有线及无线耳机', 1, TRUE, '2026-08-22 09:15:00+08'),
    ('饮料', '矿泉水、果汁等饮料', 2, TRUE, '2026-08-23 14:20:00+08'),
    ('零食', '坚果、饼干等休闲食品', 2, TRUE, '2026-08-24 16:45:00+08'),
    ('服装', '上衣、裤子等服装', 3, TRUE, '2026-08-25 10:10:00+08'),
    ('鞋子', '运动鞋、休闲鞋等鞋类', 3, FALSE, '2026-08-26 13:00:00+08'),
    ('水具', '水杯、保温杯等用品', 4, TRUE, '2026-08-23 14:20:00+08'),
    ('技术图书', '编程、计算机等技术类图书', 5, TRUE, '2026-08-23 14:20:00+08'),
    ('户外装备', '登山、露营等户外装备', 6, FALSE, '2026-08-26 13:00:00+08'),
    ('面部护理', '面霜、乳液等面部护理产品', 7, TRUE, '2026-08-23 14:20:00+08'),
    ('猫用品', '猫咪使用的各类用品', 8, TRUE, '2026-08-23 14:20:00+08');
```

```sql
-- 商品表
CREATE TABLE products (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, -- 商品ID
    category_id INTEGER NOT NULL REFERENCES categories(id), -- 所属分类ID
    name VARCHAR(50) NOT NULL, -- 商品名称
    description TEXT, -- 商品描述
    price NUMERIC(10, 2) CHECK (price >= 0), -- 商品价格
    stock INTEGER CHECK (stock >= 0), -- 库存
    is_active BOOLEAN DEFAULT FALSE, -- 是否上架
    attributes JSONB, -- 商品扩展数据
    created_at TIMESTAMPTZ DEFAULT NOW(), -- 创建时间
    updated_at TIMESTAMPTZ DEFAULT NOW() -- 更新时间
);

INSERT INTO products (category_id, name, description, price, stock, is_active, attributes)
VALUES
    (9, '华为 Mate 70', '华为旗舰智能手机', 4999.00, 25, TRUE, '{"brand": "华为", "color": "曜石黑", "storage": "512GB"}'),
    (10, '小米平板 7', '小米平板电脑', 2499.00, 40, TRUE, '{"brand": "小米", "color": "星光蓝", "storage": "256GB"}'),
    (11, '无线蓝牙耳机', NULL, 299.00, 0, TRUE, '{"brand": "SoundX", "color": "白色", "type": "入耳式"}'),
    (12, '农夫山泉矿泉水', '550ml 饮用天然水', 2.50, 200, TRUE, '{"brand": "农夫山泉", "volume": "550ml"}'),
    (13, '每日坚果', '混合坚果零食', 39.90, 80, TRUE, '{"brand": "良品铺子", "weight": "750g"}'),
    (14, '纯棉短袖', '夏季纯棉圆领短袖', 79.00, 120, TRUE, '{"material": "100%棉", "color": "白色", "size": "L"}'),
    (15, '运动鞋', '轻便透气跑步鞋', 299.00, 35, TRUE, '{"brand": "Nike", "color": "黑白", "size": "42"}'),
    (16, '不锈钢保温杯', NULL, 89.00, 60, TRUE, '{"material": "不锈钢", "volume": "500ml"}'),
    (17, 'Python 编程入门', 'Python 编程基础教程', 59.90, 30, TRUE, '{"author": "张三", "pages": 368}'),
    (18, '登山背包', '适合徒步和短途登山的背包', 199.00, 18, TRUE, '{"brand": "户外探索", "volume": "30L"}'),
    (19, '保湿面霜', '日常补水保湿面霜', 129.00, 50, FALSE, '{"brand": "护肤研究所", "volume": "50g"}'),
    (20, '猫抓板', NULL, 35.00, 0, TRUE, '{"material": "瓦楞纸", "size": "40x30cm"}');
```

```sql
-- 订单表
CREATE TABLE orders (
    id INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY, -- 订单ID
    user_id INTEGER NOT NULL REFERENCES users(id), -- 下单用户ID
    address_id INTEGER REFERENCES addresses(id), -- 收货地址ID
    total_amount NUMERIC(12, 2) CHECK (total_amount >= 0), -- 订单金额
    status VARCHAR(20) NOT NULL, -- 订单状态
    remark TEXT, -- 买家备注
    created_at TIMESTAMPTZ DEFAULT NOW(), -- 创建时间
    updated_at TIMESTAMPTZ DEFAULT NOW() -- 更新时间
);

INSERT INTO orders (user_id, address_id, total_amount, status, remark)
VALUES
    (1, 1, 5298.00, 'completed', '请尽快发货'),
    (1, 2, 299.00, 'paid', NULL),
    (6, 3, 2499.00, 'shipped', '工作日送货'),
    (6, 4, 159.80, 'completed', NULL),
    (7, 5, 498.00, 'pending', '送货前请电话联系'),
    (8, 6, 59.90, 'cancelled', NULL),
    (9, NULL, 129.00, 'paid', '虚拟商品，无需配送'),
    (10, 7, 199.00, 'shipped', NULL),
    (10, 8, 338.00, 'completed', '放在门卫处');


-- 订单明细表
CREATE TABLE order_items (
    id INTEGER PRIMARY KEY GENERATED ALWAYS AS IDENTITY, -- ID
    order_id INTEGER REFERENCES orders(id) NOT NULL, -- 所属订单ID
    product_id INTEGER REFERENCES products(id) NOT NULL, -- 商品ID
    quantity INTEGER CHECK(quantity > 0) NOT NULL, -- 数量
    unit_price NUMERIC(10, 2) NOT NULL, -- 单价
    created_at TIMESTAMP DEFAULT NOW() -- 创建时间
);

INSERT INTO order_items
    (order_id, product_id, quantity, unit_price)
VALUES
    -- 订单 1：华为 Mate 70 + 每日坚果
    (1, 1, 1, 4999.00),
    (1, 5, 2, 39.90),
    -- 订单 2：无线蓝牙耳机
    (2, 3, 1, 299.00),
    -- 订单 3：小米平板 7
    (3, 2, 1, 2499.00),
    -- 订单 4：每日坚果 + 纯棉短袖
    (4, 5, 2, 39.90),
    (4, 6, 1, 79.00),
    -- 订单 5：运动鞋 + 保温杯
    (5, 7, 1, 299.00),
    (5, 8, 1, 89.00),
    (5, 4, 4, 2.50),
    -- 订单 6：Python 编程入门
    (6, 9, 1, 59.90),
    -- 订单 7：保湿面霜
    (7, 11, 1, 129.00),
    -- 订单 8：登山背包
    (8, 10, 1, 199.00),
    -- 订单 9：运动鞋 + 猫抓板
    (9, 7, 1, 299.00),
    (9, 12, 1, 35.00);
```

</details>


## 基础概念

### 字段类型

- `INTEGER` 32位整数
- `BIGINT` 64位整数
- `VARCHAR(n)` 可变长度字符串
- `TEXT` 可变长度字符串 (无需指定长度)
- `DECIMAL(总位数, 小数位数)` 精确的定点数类型, 适合金额计算的场景
- `NUMERIC(总位数, 小数位数)` 与 `DECIMAL` 等价
- `BOOLEAN` 布尔类型, 只能是 `TRUE` 或 `FALSE`
- `DATE` 日期 (年月日), 不包含时间和时区
- `TIMESTAMP` 时间戳 (不带时区)
- `TIMESTAMPTZ` 时间戳 (带时区)

!> TODO: 数据类型补完 — `TEXT`（不指定长度限制）、`NUMERIC`/`DECIMAL`（精确小数，金额必用）、`BOOLEAN`、`DATE`  
!> TODO: `JSON` / `JSONB`（PG 强项，agent 重点：状态 / 工具调用结果 / LLM 结构化输出存储，建议提前学、单独练）

### 伪类型

- `SERIAL` 是<mark>INTEGER</mark>+<mark>SEQUENCE</mark>+<mark>DEFAULT nextval()</mark>的简写
  - <mark class="purple">SEQUENCE 是一个专门负责生成连续序号的数据库对象</mark>

### 约束与关键词定义

- `PRIMARY KEY` 主键, 唯一, 非空
- `REFERENCES 外表名(外表字段)` 外键, 可以引用另一个表的主键或唯一键
  - 等价 <mark class="green">SQL 语句</mark> `FOREIGN KEY (字段) REFERENCES 外表名(外表字段);`
- `NOT NULL` 不可设置为null, 但可以是空字符串
- `UNIQUE` 不能重复
- `DEFAULT xxx` 默认值
- `GENERATED ALWAYS AS IDENTITY` 由数据库自动生成, 默认情况下不能设置
  - 如果是 `OVERRIDING SYSTEM VALUE` 则可以显式插值
- `GENERATED BY DEFAULT AS IDENTITY` 由数据库自动生成, 但可以显式的设置值

!> TODO: 约束进阶 — `FOREIGN KEY` 外键（建立表之间的引用关系；常用于一对多，可配合 UNIQUE 实现一对一）、`CHECK`（如 `CHECK(age >= 0)` 检查约束）

### 面试题

> **`INTEGER` 和 `BIGINT` 有什么区别?**  
*INTEGER 表示32位整数, 取值范围是正负 21 亿*  
*BIGINT 表示64位整数, 取值范围是正负 922 万亿亿*  
*他俩主要就是取值范围和存储空间的区别*

> **`VARCHAR(50)` 指的是什么意思?**  
*VARCHAR 表示可变字符串, 括号里的数字表示最多存储50个字符*

> **`PRIMARY KEY` 和 `UNIQUE` 有什么区别?**  
*前者用来表示主键, 并且一张表里只能存在一个主键, 自带 NOT NULL 属性, 后者用来表示唯一不可重复, 并且可以出现多次, 也可以为 NULL*

> **`SERIAL` 的自增原理是什么?**  
*我理解的就是做了三件事, 先创建 INTEGER 32位整数字段, 然后创建一个 SEQUENCE 用来生成和维护递增序列, 相当于一个发牌器, 然后给字段设置一个 DEFAULT nextval(), 其中nextval用来取值, 最后通过 DEFAULT 关键词设置默认值*

> **PostgreSQL 有几种自增机制, 各有什么优缺点?**  
*<mark class="blue">SERIAL</mark> 伪类型, 属于传统写法, 优点是书写简单好记, 缺点 Sequence 是隐式创建和关联的，结构管理不如 Identity 直观*  
*<mark class="">GENERATED xxx AS IDENTITY</mark> 更现代的写法, Identity 列, 语义明确, 有 <mark class="blue">ALWAYS</mark> 和 <mark class="blue">BY DEFAULT</mark> 两种规则, 默认由数据库自动生成, 前者是默认不可指定, 后者是可以显式的指定*

> **如何判断一个 NULL 类型?**  
*可以使用 <mark>WHERE 字段 IS NULL</mark> 或者 <mark>WHERE 字段 IS NOT NULL</mark> 来判断, <line>不能直接通过 NULL = NULL</line>, 否则就会得到一个 UNKNOWN, 因为 sql 里 NULL 与任何值比较, 都会得到一个 UNKNOWN*


## 对象操作 (DDL)

### DATABASE 数据库

- 查询所有数据库: `\l`
- 切换指定数据库: `\c 数据库名`
- 创建数据库: `CREATE DATABASE 数据库名;`
- 删除数据库: `DROP DATABASE 数据库名;`

```sql
-- 新建一个测试数据库
CREATE DATABASE test;
```

```sql
-- 删除测试数据库
DROP DATABASE test;
```

### TABLE 表

- 查看当前数据库有哪些表: `\dt`
- 查看表结构: `\d 表名`
- 创建表: `CREATE TABLE 表名 (字段1, 字段2);`
- 删除表: `DROP TABLE 表名;`

```sql
-- 创建用户表
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);
```

```sql
-- 删除用户表
DROP TABLE users;
```

### ALTER TABLE 修改表结构

!> TODO: 加列 / 删列 / 改类型 / 重命名

### TRUNCATE 清空表

!> TODO: 快速清空表，通常比 DELETE 更快；测试 / 重置数据常用

## 数据操作 (DML)

### INSERT 插入

> 语法模板: `INSERT INTO 表名 (字段1, ...) VALUES (值1, ...), (值2, ...);`

```sql
-- 插入一条数据
INSERT INTO users (username, age)
VALUES ('admin', 0);

-- 插入多条数据
INSERT INTO users (username, nickname, age)
VALUES ('jack88', 'jack', 20),
       ('ben66', 'ben', 22);
```

#### RETURNING 返回结果

- **适用语句：** INSERT、UPDATE、DELETE（返回受影响行的字段值；SELECT 本身就返回数据，故不需要 RETURNING）

> 语法模板: `INSERT INTO 表名 (字段)
VALUES (值)
RETURNING 字段1, 字段2;`

```sql
-- 插入一条数据并返回对应id
INSERT INTO users (username, age)
VALUES ('tom', 60)
RETURNING id;
```

### SELECT 查询

> 语法模板: `SELECT 字段 FROM 表名;`

```sql
-- 查询单个字段
SELECT username FROM users;

-- 查询多个字段
SELECT id, username, balance FROM users;

-- 查询所有字段
SELECT * FROM users;
```

#### WHERE 条件

- **适用语句：** SELECT、UPDATE、DELETE（用于筛选要操作的数据行）

> 语法模板: `SELECT 字段 FROM 表名 WHERE 条件;`

- 条件:
  - `=` 等于
  - `<>` `!=` 不等于
  - `>` 大于
  - `<` 小于
  - `>=` 大于等于
  - `<=` 小于等于

```sql
-- 查询所有下架的商品
SELECT * FROM products WHERE is_active = FALSE;

-- 查询价格超过1000的商品
SELECT * FROM products WHERE price > 1000;
```

- 多个条件:
  - `AND` 并且
  - `OR` 或者

```sql
-- 查询价格大于100并且库存充足的商品
SELECT * FROM products WHERE price > 100 AND stock > 0;

-- 查询已下架或库存为0的商品
SELECT * FROM products WHERE is_active = FALSE OR stock = 0;
```

#### LIKE / ILIKE 模糊匹配

1. `%`可以用来表示<mark>任意字符</mark> <mark>0 到 n 个字符</mark>

> 语法模板: `SELECT * FROM 表名 WHERE 字段 LIKE '%值%';`

```sql
-- 查询名称包含"小米"的商品
SELECT * FROM products WHERE name LIKE '%小米%';

-- 查询名称以"耳机"结尾的商品
SELECT * FROM products WHERE name LIKE '%耳机';
```

2. `_`只能用来表示<mark>单个字符</mark>

> 语法模板: `SELECT * FROM 表名 WHERE 字段 LIKE '值_值';`

```sql
-- 查询小米所有系列的平板
SELECT * FROM products WHERE name LIKE '小米平板 _';
```

3. `ILIKE` 则用来表示不区分大小写

> 语法模板: `SELECT * FROM 表名 WHERE 字段 ILIKE 值`

```sql
-- 查询所有 meta 系列的商品
SELECT * FROM products WHERE name ILIKE '%mate%';
```

#### IN / NOT IN 集合匹配

如果我要查询<mark class="orange">已经支付、发货或完成</mark>状态的订单

那我就得写 `SELECT * FROM orders WHERE status = 'paid' OR status = 'shipped' OR status = 'completed';`

如果要查询的很多, 就得写一堆 `OR`, 非常的繁琐

此时我们可以用 `IN ('paid', 'shipped', 'completed')` 的方式来判断

> 语法模板: `SELECT * FROM 表名 WHERE 字段 IN (值1, 值2, ...);`

```sql
-- 查询有没有用户名是 admin 或 root 的数据
SELECT * FROM orders WHERE status IN ('paid', 'shipped', 'completed');
```

同理, 如果我要查询<mark class="orange">未取消、未完成</mark>的订单

就得写`SELECT * FROM orders WHERE status != 'cancelled' AND status != 'completed';`

但我们也可以用 `NOT IN ('cancelled', 'completed')` 来代替

> 语法模板: `SELECT * FROM 表名 WHERE 字段 NOT IN (值1, 值2, ...);`

```sql
-- 查询用户不等于 admin 或 root 的数据
SELECT * FROM orders WHERE status NOT IN ('cancelled', 'completed');
```

#### BETWEEN / NOT BETWEEN 区间匹配

如果我们想判断一个字段的值, 是否在一个区间内, 此时可以使用 `BETWEEN`

比如我们要查询价格在 50 到 300 元之间的商品

> 语法模板: `SELECT 字段 FROM 表名 WHERE 字段 BETWEEN 最小值 AND 最大值;`

```sql
-- 查询价格在 50 到 300 元之间的商品
SELECT * FROM products WHERE price BETWEEN 50 AND 300;

-- 等价于这种写法
SELECT * FROM products WHERE price >= 50 AND price <= 300;
```

与之对应的还有 `NOT BETWEEN` 不在这个区间内

> 语法模板: `SELECT 字段 FROM 表名 WHERE 字段 NOT BETWEEN 最小值 AND 最大值;`

```sql
-- 查询价格不在 100 至 500 元之间的商品
SELECT * FROM products WHERE price NOT BETWEEN 100 AND 500;

-- 等价于这种写法
SELECT * FROM products WHERE price < 100 OR price > 500;
```


#### NULL 与空值处理

在 PostgreSQL 里 `NULL` 表示没有值 / 缺失值, 如果 `NULL` 跟任何数据做判断比较的话, 永远都会得到一个 `UNKNOWN`

比如 `NULL = NULL` 也是 `UNKNOWN`

<line class="">正确的做法是通过 `IS NULL` 和 `IS NOT NULL` 来判断</line>

```sql
-- 查询电话号为空的用户
SELECT * FROM users WHERE phone IS NULL;

-- 查询电话号不为空的用户
SELECT * FROM users WHERE phone IS NOT NULL;
```

#### COALESCE 空值替换

`COALESCE()` 函数的作用是从左到右返回第一个非 NULL 的值, 如果所有值都是 NULL，则返回 NULL

> 语法模板：`COALESCE(值1, 值2, ...)`

```sql
SELECT id, name, COALESCE(description, '暂无描述') FROM products;
```

#### DISTINCT 去重

如果想对查询结果去重, 则需要使用 DISTINCT 来实现, 并且 DISTINCT 要跟在 SELECT 后面

> 语法模板: `SELECT DISTINCT 字段 FROM 表名;`

```sql
-- 查询所有用户的年龄并去重
SELECT DISTINCT age FROM users;
```

#### CASE 条件表达式

可以把 CASE 理解为 SQL 里的 `if / else`

> 模板语法:
`CASE`  
`WHEN 条件1 THEN 结果1`  
`WHEN 条件2 THEN 结果2`  
`ELSE 结果3`  
`END`

```sql
-- 假如我们想根据实际库存判断商品状态
SELECT
    id,
    name,
    stock,
    CASE
        WHEN stock = 0 THEN '缺货'
        WHEN stock < 20 THEN '库存紧张'
        ELSE '库存充足'
    END AS stock_status
FROM products;
```


#### COUNT / SUM / AVG / MIN / MAX 聚合函数

> 聚合函数可以帮助我们把多行数据计算成一个结果

1. <line>COUNT 统计数量</line>

```sql
-- 统计有多少个商品
SELECT COUNT(*) FROM products;

-- 统计有多少个描述不为空的商品
SELECT COUNT(description) FROM products;
```

2. <line>SUM 求和</line>

```sql
-- 把所有商品库存加起来
SELECT SUM(stock) FROM products;
```

3. <line>AVG 平均值</line>

```sql
-- 查询所有商品的平均价格
SELECT AVG(price) from products;
```

4. <line>MIN 最小值</line>

```sql
-- 查询最便宜的商品
SELECT MIN(price) from products;
```

5. <line>MAX 最大值</line>

```sql
-- 查询最贵的商品
SELECT MAX(price) from products;
```


#### GROUP BY 分组

`GROUP BY` 的作用是按照指定字段进行分组，然后对每个分组进行统计。

!> 并且`SELECT`查询的字段要<line class="red">满足以下条件之一</line>：<mark class="red">出现在 `GROUP BY` 中</mark> 或 <mark class="red">被聚合函数处理</mark>

<line>分组后，每个组产生一条结果；聚合函数负责计算这个组的统计值。</line>

> 语法模板: `SELECT 分组字段, 聚合函数(字段) FROM 表名 GROUP BY 分组字段`

```sql
-- 每个分类有多少商品
SELECT category_id, COUNT(*) FROM products GROUP BY category_id;

-- 每个分类的平均价格
SELECT category_id, AVG(price) FROM products GROUP BY category_id;

-- 每个分类的总库存
SELECT category_id, SUM(stock) FROM products GROUP BY category_id;
```


#### HAVING 过滤分组

官方定义: `WHERE`在分组前过滤, `HAVING`在`GROUP BY`分组之后过滤

> 语法模板: `SELECT 分组字段, 聚合函数(统计字段) FROM 表名 GROUP BY 分组字段 HAVING 条件`

```sql
-- 只查看平均价格超过200元的商品分类
SELECT category_id, AVG(price) AS avg_price FROM products GROUP BY category_id HAVING AVG(price) > 200;

-- 查询商品数量超过2个的商品分类
SELECT category_id FROM products GROUP BY category_id HAVING COUNT(*) > 2;
```

#### ORDER BY 排序

- **适用语句：** SELECT

> 语法模板: `SELECT 字段 FROM 表名 ORDER BY 字段;`

- `ASC` 升序 (默认)
- `DESC` 降序

```sql
-- 商品价格从低到高
SELECT * FROM products ORDER BY price;
SELECT * FROM products ORDER BY price ASC;

-- 商品价格从高到低
SELECT * FROM products ORDER BY price DESC;
```

#### LIMIT / OFFSET 分页

- **适用语句：** SELECT（<mark class="yellow">PostgreSQL 的 UPDATE / DELETE 不支持 LIMIT / OFFSET</mark>，需分页请改用其他写法）

> 语法模板: `SELECT 字段 FROM 表名 LIMIT 数量 OFFSET 数量;`

- `LIMIT` 限制返回条数
- `OFFSET` 跳过数据

```sql
-- 按商品价格从高到低查询前 5 个商品
SELECT * FROM products ORDER BY price DESC LIMIT 5;

-- 查询第 2 页, 每页 5 条
SELECT * FROM products ORDER BY price DESC LIMIT 5 OFFSET 5;
```

### UPDATE 修改

> 语法模板: `UPDATE 表名 SET 字段1 = 新值1, 字段2 = 新值2 WHERE 条件;`

```sql
-- 将库存为 0 的商品全部下架
UPDATE products SET is_active = FALSE WHERE stock = 0;

-- 把所有账户余额是 NULL 的用户都修复成 0
UPDATE users SET balance = 0 WHERE balance IS NULL;

-- 给50岁以上的用户增加500元余额
UPDATE users SET balance = balance + 500 WHERE age > 50;
```

### DELETE 删除

> 语法模板: `DELETE FROM 表名 WHERE 条件;`

```sql
-- 删除某个用户
DELETE FROM users WHERE id = 9;
```


## 连表查询

### JOIN

#### INNER JOIN（取两表交集）

官方定义：对于左表的一行，只有右表存在满足 JOIN 条件的行时，才会产生连接结果。

> 语法模板: `SELECT 表1.字段, 表2.字段 FROM 表1 INNER JOIN 表2 ON 表1.字段 = 表2.字段`

```sql
-- 查询所有订单, 并显示下单用户的用户名
SELECT
    orders.id, users.username
FROM orders
INNER JOIN users
    ON orders.user_id = users.id;

-- 统计每个用户下了多少单
SELECT
    u.id,
    u.username,
    COUNT(*) AS order_count
FROM users AS u
INNER JOIN orders AS o
    ON u.id = o.user_id
GROUP BY
    u.id,
    u.username;
```

!> 如果只写`JOIN`也是可以的, 因为`JOIN`默认就是`INNER JOIN`

#### LEFT JOIN（左表全保留，右表无匹配补 NULL）

`LEFT JOIN`会保留左表中的所有记录, 而`INNER JOIN`只会保留交集, 但如果出现没关联到的数据, 则会给对应字段默认填充`NULL`

```sql
-- 查询所有用户, 以及每个用户的订单数量
SELECT
    u.id,
    u.username,
    COUNT(o.id) AS order_count
FROM users AS u
LEFT JOIN orders AS o
    ON u.id = o.user_id
GROUP BY
    u.id,
    u.username;
```

对于`LEFT JOIN`来说, 还有一个地方需要额外补充, <mark class="yellow">那就是`ON`条件和`WHERE`条件会产生不同的结果</mark>


- ON→ 决定「能不能关联上」
- WHERE→ 决定「关联完成后留不留下」

```sql
-- 没有条件的情况下: 会查询出所有用户及其订单
SELECT
    u.username,
    o.id,
    o.status
FROM users AS u
LEFT JOIN orders AS o
    ON u.id = o.user_id;

-- 条件在 ON: 所有用户都会保留, 但只有 paid 状态的订单可以关联
SELECT
    u.username,
    o.id,
    o.status
FROM users AS u
LEFT JOIN orders AS o
    ON u.id = o.user_id
    AND o.status = 'paid';

-- 条件在 WHERE: 关联完成后, 只保留 paid 状态的数据
SELECT
    u.username,
    o.id,
    o.status
FROM users AS u
LEFT JOIN orders AS o
    ON u.id = o.user_id
WHERE o.status = 'paid';
```

#### 多表连接

> 语法模板: `SELECT ...`  
`FROM 表1`  
`JOIN 表2 ON 表1.xxx = 表2.xxx`  
`JOIN 表3 ON 表2.xxx = 表3.xxx;`

在真实的业务场景里, 会有一次查询多表的情况, 比如下面这个情况, 就需要一次性查询多个表

```sql
-- 查询所有订单的完整信息(订单ID, 用户名, 收货城市, 订单状态)
SELECT
    o.id AS order_id,
    u.username,
    a.city,
    o.status AS order_status
FROM orders AS o
LEFT JOIN addresses AS a
    ON o.address_id = a.id
LEFT JOIN users AS u
    ON o.user_id = u.id;

-- 查询所有订单的完整信息(订单ID, 用户名, 商品名称, 购买数量, 订单状态)
SELECT
    o.id AS order_id,
    u.username,
    p.name AS product_name,
    i.quantity AS product_num,
    o.status AS order_status
FROM orders AS o
LEFT JOIN order_items AS i
    ON o.id = i.order_id
LEFT JOIN products AS p
    ON i.product_id = p.id
LEFT JOIN users AS u
    ON o.user_id = u.id;

```

#### `JOIN` + 聚合

```sql
-- 查询所有用户的订单数量
SELECT
    u.id,
    u.username,
    COUNT(o.id) AS order_num
FROM users AS u
LEFT JOIN orders AS o
    ON u.id = o.user_id
GROUP BY
    u.id,
    u.username;

-- 查询所有用户, 显示用户名和已完成的订单数量
SELECT
    u.id,
    u.username,
    COUNT(o.id) AS finished_order_num
FROM users AS u
LEFT JOIN orders AS o
    ON u.id = o.user_id
    AND o.status = 'completed'
GROUP BY
    u.id,
    u.username;
```

#### 自连接

Self JOIN 的核心就是, 同一张表在一次查询里当两张表来使用, 最经典的应用场景就是无限极分类查询

```sql
-- 查询每个子分类以及父级分类的名称
SELECT
    c1.name,
    c2.name AS parent_name
FROM categories AS c1
LEFT JOIN categories AS c2
    ON c1.parent_id = c2.id;

-- 查询所有一级分类, 和对应的子分类的数量
SELECT
    c1.id,
    c1.name,
    COUNT(c2.id) AS sub_count
FROM categories AS c1
LEFT JOIN categories AS c2
    ON c1.id = c2.parent_id
WHERE c1.parent_id IS NULL
GROUP BY
    c1.id,
    c1.name;
```

### 子查询与集合

#### 子查询（IN / EXISTS）

- IN 是否在这个集合里
- NOT IN 是否不在这个集合里
- EXISTS 有没有这么一条记录
- NOT EXISTS 是否没这么一条记录

```sql
-- 使用 IN 查询下过订单的用户
SELECT
    id,
    username
FROM users
WHERE id IN (
    SELECT user_id FROM orders
);

-- 使用 EXISTS 查询下过订单的用户
SELECT
    id,
    username
FROM users AS u
WHERE EXISTS (
    SELECT 1
    FROM orders AS o
    WHERE u.id = o.user_id
);
```

#### UNION / UNION ALL

1. UNION 会自动去重

```sql
-- 查询所有 completed 订单和 cancelled 订单，只显示 id 和 status
SELECT id, status FROM orders WHERE status = 'completed'
UNION
SELECT id, status FROM orders WHERE status = 'cancelled';
```

2. UNION ALL 会保留所有结果

```sql
-- 查询所有 completed 和 paid 订单，只显示 user_id
SELECT user_id FROM orders WHERE status = 'completed'
UNION ALL
SELECT user_id FROM orders WHERE status = 'paid';
```

!> `UNION`/`UNION ALL`后面的`ORDER BY`/`LIMIT`, 默认作用于整个`UNION`结果, 而不是只作用于右边的`SELECT`。

```sql
SELECT
    user_id
FROM orders
WHERE status = 'completed'
UNION
SELECT
    user_id
FROM orders
WHERE status = 'paid'
-- 后面的操作, 会针对前面的整体, 而不是只针对第二个查询语句
ORDER BY user_id DESC
LIMIT 3;
```

## 写入进阶

### 事务（必会）

- `BEGIN` / `COMMIT` / `ROLLBACK`（保证多步写操作原子一致）

#### 事务隔离级别（知道即可）

- 4 种 + 默认 `READ COMMITTED`，大致了解脏读 / 不可重复读 / 幻读

### 冲突处理（upsert）

#### ON CONFLICT ... DO NOTHING

#### ON CONFLICT ... DO UPDATE（agent 写接口幂等 / 重试重点）


## 性能与对象（够用即可）

### 索引

#### CREATE INDEX（单列 · 复合 · 唯一索引）

#### EXPLAIN 入门（会跑、能看 Seq Scan 全表扫 vs Index Scan，不深入执行计划）

### 序列管理

- `CREATE SEQUENCE` / `nextval()` / `currval()`（已讲 SERIAL 依赖序列，顺带提一句）

### 生成列

- `GENERATED ALWAYS AS (表达式) STORED`（值由其他列计算，写入时存储）

### 视图

- `CREATE VIEW`（简化复杂查询、隐藏实现）


## Agent 场景（专属）

### 向量检索（pgvector）

- `pgvector` 扩展（embedding 向量存储 + 相似度搜索 `<=>` / `<#>`），agent 记忆 / RAG 召回的核心能力

### 对话历史表设计

- `sessions` + `messages` 两表 + JSONB 存 `tool_calls` / LLM 结构化输出（agent 开发最高频的建表模式）
